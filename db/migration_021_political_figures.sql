-- =====================================================================
-- جریان — مهاجرت ۰۲۱: تب «چهره‌های سیاسی» در بخش «یادداشت‌ها»
-- این فایل را کامل در Supabase SQL Editor پیست و اجرا کنید.
-- ایمن برای اجرای چندباره (idempotent).
--
-- زمینه: تب جدید «چهره‌های سیاسی» توی بخش «یادداشت‌ها» (سابقاً «افراد
-- و مجموعه‌ها») پست‌های همون کانال‌های موجود (type='person') رو نشون
-- می‌ده، اما فقط اونایی که مدیر صریحاً براشون این عضویت رو فعال کرده،
-- عیناً هم‌الگو با show_in_news/show_in_cyberspace/show_in_people
-- (migration_007) — یه کانال می‌تونه هم‌زمان توی چند حوزه باشه.
-- پیش‌فرض false: هیچ کانالی خودکار وارد این تب نمی‌شه، مدیر باید از
-- پنل «مدیریت کانال‌ها» دستی تیک بزنه.
-- =====================================================================

alter table channels add column if not exists show_in_political_figures boolean not null default false;

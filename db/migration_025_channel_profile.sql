-- =====================================================================
-- جریان — مهاجرت ۰۲۵: پروفایل کنشگر (صاحب کانال + تعداد مخاطب)
-- این فایل را کامل در Supabase SQL Editor پیست و اجرا کنید.
-- ایمن برای اجرای چندباره.
--
-- زمینه: تب جدید «افراد و مجموعه‌ها» (زیر بخش «کنشگری مجازی») یک گالری
-- معرفی از کانال‌های show_in_cyberspace=true نشون می‌ده. اسم/عکس/دسته‌بندی
-- از قبل روی channels هست؛ میزان فعالیت هم سمت کلاینت از posts محاسبه
-- می‌شه. فقط «صاحب کانال» و «تعداد مخاطب» فیلد جدیدن — چون هیچ کالکتوری
-- این دو رو زنده نمی‌گیره (نه ایتا نه تلگرام API عمومی)، دستی از پنل
-- «مدیریت کانال‌ها» پر می‌شن، نه خودکار.
-- =====================================================================

alter table channels add column if not exists owner_name text;
alter table channels add column if not exists subscriber_count integer;

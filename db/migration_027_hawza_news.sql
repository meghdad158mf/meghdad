-- =====================================================================
-- جریان — مهاجرت ۰۲۷: تب «اخبار حوزه»
-- این فایل را کامل در Supabase SQL Editor پیست و اجرا کنید.
-- ایمن برای اجرای چندباره.
--
-- زمینه: تب جدید «اخبار حوزه» (داخل بخش «اخبار و رویدادها») فقط از
-- سایت‌های خبری اختصاصی حوزه تغذیه می‌شه، نه از تطبیق کلیدواژه روی کل
-- اخبار. کانال‌های platform=website که این ستون روشون true باشه، از تب
-- عمومی «وب‌سایت‌ها» مستثنا و به‌جاش توی «اخبار حوزه» نشون داده می‌شن —
-- هم‌الگو با استقلال کانال بله («ادعاها و شایعات») از فید عمومی.
-- =====================================================================

alter table channels add column if not exists show_in_hawza boolean not null default false;

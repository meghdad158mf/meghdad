# جریان — راهنمای پروژه برای Claude

این فایل به‌صورت خودکار توسط Claude Code خونده می‌شه. هدفش اینه که هر جلسه‌ی جدید (چت جدید) بدون نیاز به توضیح دوباره، بفهمه پروژه چیه، تا کجا پیش رفته، و چی مونده.

## پروژه چیه؟

**«جریان»** یک سامانه‌ی رصد رسانه‌ایه برای **اداره‌ی رصد و راهبری سیاسی‌اجتماعی حوزه‌ی علمیه‌ی خراسان**.

قبل از این پروژه، یک سیستم مستقل و کاملاً کارکننده برای پایش تلگرام از قبل وجود داشت (Python + FastAPI + SQLite + Telethon، روی سیستم شخصی کاربر). این پروژه اون سیستم رو با ایتا یکی کرد و کل زیرساخت رو از «سیستم شخصی» به «کاملاً ابری و رایگان» منتقل کرد.

### ساختار محتوایی: ۴ حوزه‌ی رصد (تصمیم مرحله‌ی دوم — جایگزین تفکیک اولیه‌ی ایتا/تلگرام)

سایت اولش بر اساس پلتفرم (پایش ایتا / پایش تلگرام) طراحی شده بود. بعداً کاربر این رو به یک ساختار بالادست‌تر تغییر داد — ۴ «حوزه‌ی رصد» که نویگیشن اصلی سایت می‌شن:

1. **رصد فضای مجازی** — تنها حوزه‌ی فعال فعلاً. همه‌ی کانال‌های ایتا + تلگرام با هم (`platform in ('eitaa','telegram')`)، فارغ از این‌که کانال خبرگزاری باشه یا شخص. همون داشبورد/پست‌ها/آنالیزی که الان ساخته شده، اساساً همینه.
2. **رصد اخبار** — ⏸️ فعلاً غیرفعال به‌خواست کاربر (فقط آیتم نویگیشن با نشان «به‌زودی»، بدون کالکتور). قرار بود بخشی از وب‌سایت‌های خبری + بخشی از کانال‌های فضای مجازی که نوعشون خبرگزاریه (`type='news_agency'`) باشه — این طراحی برای وقتی که کاربر دوباره مطرحش کنه نگه داشته شده.
3. **رصد فعالیت مدارس** — ⏸️ فعلاً غیرفعال به‌خواست کاربر (فقط آیتم نویگیشن). منبعش قراره یک فایل اکسل باشه که کاربر می‌ده؛ جدولش هنوز ساخته نشده.
4. **رصد برنامه تشکل‌ها** — ⏸️ فعلاً غیرفعال به‌خواست کاربر (فقط آیتم نویگیشن). همون مدل اکسل مثل مدارس.

علاوه بر این ۴ حوزه، یک **«بخش تحلیلی»** هم هست که شامل تحلیل هوش مصنوعی (هنوز فقط UI) + **کتابخانه‌ی مجلات** می‌شه (مجله‌های چاپی که مدیر به‌صورت PDF یا چند عکس آپلود می‌کنه تا بقیه بخونن).

دسته‌بندی‌های موضوعی فعلی (سیاسی/اجتماعی/اقتصادی/فرهنگی/بین‌الملل/تحلیل راهبردی — جدول `categories`) محور جدایی از حوزه‌ی رصدن و دست‌نخورده می‌مونن؛ فقط قراره اسم‌هاشون توسط کاربر عوض بشه (نیاز به تغییر اسکیما نداره، فقط UI مدیریت دسته‌ها که هنوز ساخته نشده).

## نیازهای اصلی کاربر (زمینه‌ی تصمیم‌های معماری)

- **بدون سیستم شخصی** — همه‌چیز باید ابری/میزبانی‌شده باشه
- **به‌روزرسانی دوره‌ای** — حداقل هر چند ساعت یک‌بار (الان: هر ۲ ساعت)
- **کمترین هزینه** — ترجیحاً رایگان
- **سرعت بالا** — دیتابیس واقعی، نه فایل ساده
- **قابلیت آنالیزهای متنوع** — SQL واقعی، نه محدود به گزارش‌های از پیش تعریف‌شده
- کانال‌ها باید نوع داشته باشن: **خبرگزاری** یا **شخصیت/فرد**
- هر کانال/شخصیت باید بتونه **صفحه‌ی آنالیز اختصاصی خودش** رو داشته باشه (هنوز پیاده نشده — نگاه کن به «موارد باز»)

## معماری فعلی

```
GitHub Actions (هر ۲ ساعت، cron)
    │
    ├── scripts/collect_eitaa.py     ── اسکرپ HTML صفحات عمومی eitaa.com (بدون نیاز به API)
    └── scripts/collect_telegram.py  ── Telethon، polling دوره‌ای (نه listener زنده)
    │
    ▼
Supabase (Postgres + PostgREST رایگان)
    │  دو نقش Postgres: app_admin / app_viewer
    │  ورود با پسورد مشترک از طریق تابع public.login() (pgcrypto برای هش، pgjwt برای توکن)
    │  RLS: بیننده فقط خواندن، مدیر خواندن/نوشتن
    ▼
design/ita-monitoring-prototype.html  ── فرانت‌اند تک‌فایلی، مستقیم با fetch به PostgREST وصله
```

هیچ بک‌اند سفارشی (FastAPI و مشابه) وجود نداره — فرانت‌اند مستقیم با Supabase حرف می‌زنه.

## ساختار ریپو

```
db/schema.sql                          اسکیمای کامل (جدول‌ها، RLS، توابع login/change_password) — روی این فایل کامل دوباره اجرا نکن! رمزها رو ریست می‌کنه (بخش «نکات عملیاتی» رو ببین)
db/migration_002_domains_magazines.sql جدول domains + magazines + پلتفرم website + Storage bucket — ایمن برای اجرای چندباره، رمز دست نمی‌زنه
db/seed_telegram_channels.sql          ۴ کانال پیش‌فرض تلگرام
scripts/collect_eitaa.py               کالکتور ایتا (اجرا هر ۲ ساعت با GitHub Actions)
scripts/collect_telegram.py            کالکتور تلگرام (اجرا هر ۲ ساعت)
scripts/telegram_session_to_string.py  ابزار یک‌بارمصرف محلی برای گرفتن StringSession از session قدیمی
.github/workflows/collect-eitaa.yml
.github/workflows/collect-telegram.yml
design/ita-monitoring-prototype.html   فرانت‌اند کامل (نقشه‌ی سایت، ورود، داشبورد، پست‌ها، آنالیز، کانال‌ها، خروجی، تنظیمات)
.claude/skills/ui-ux-pro-max/          اسکیل طراحی نصب‌شده توسط یک سشن دیگه (چک‌لیست‌های UI/UX)
```

## جدول‌های دیتابیس (خلاصه)

- **channels**: id, platform (`eitaa`|`telegram`|`website`), username, title, type (`news_agency`|`person`), category_id, active
- **posts**: id, channel_id, platform, platform_post_id, text, media_type, posted_at, scraped_at, views, forwards, link — یکتا روی (channel_id, platform_post_id)
- **categories**: id, name, color — دسته‌بندی موضوعی (محور جدا از حوزه‌ی رصد)
- **domains** *(جدید، migration_002)*: id, key (`cyberspace`|`news`|`schools`|`orgs`), name, sort_order — فقط برای نمایش/نویگیشن؛ عضویت کانال در حوزه محاسبه‌شده‌ست، نه یک FK
- **magazines** *(جدید، migration_002)*: id, title, issue_no, publish_date, file_type (`pdf`|`images`), files (jsonb، مسیرهای Storage)، cover_url — فایل‌ها در bucket خصوصی `magazines`
- **app_config**: رمزهای هش‌شده‌ی مدیر/بیننده + رمز JWT (فقط از طریق توابع SECURITY DEFINER قابل‌خوندنه، هیچ‌وقت مستقیم API نمی‌خوره)
- ⏳ **جدول رصد مدارس/تشکل‌ها**: هنوز طراحی نشده — منتظر فایل اکسل واقعی کاربر

## وضعیت فعلی (مرحله‌به‌مرحله)

| بخش | وضعیت |
|---|---|
| طراحی UI/UX | ✅ تکمیل، یک بار هم صیقل‌کاری با ui-ux-pro-max |
| اسکیمای دیتابیس + احراز هویت دوسطحی | ✅ روی Supabase مستقر و تست‌شده |
| کالکتور ایتا | ✅ فعال، هر ۲ ساعت اجرا می‌شه، داده‌ی واقعی می‌گیره |
| کالکتور تلگرام | ✅ فعال، فقط ۴ کانال پیش‌فرض داره |
| اتصال فرانت‌اند به داده‌ی واقعی | ✅ کامل (نه دیگه mock data) |
| صفحه‌ی آنالیز اختصاصی هر کانال | ❌ هنوز نه — فقط آنالیز کلی جمعی هست |
| لیست واقعی کانال‌های تلگرام | ❌ فقط ۴ خبرگزاری عمومی پیش‌فرضه؛ کانال‌های واقعی حوزه هنوز اضافه نشدن |
| تحلیل هوش مصنوعی | ❌ فقط UI نمونه — پایپ‌لاین واقعی (n8n + Claude API) ساخته نشده |
| خروجی Excel | ❌ فقط CSV/JSON واقعی کار می‌کنه؛ دکمه‌ی Excel toast «به‌زودی» می‌ده |
| مهاجرت داده‌ی تاریخی تلگرام (messages.db قدیمی، ۱۸۳ مگابایت SQLite) | ❌ کاری روش انجام نشده |
| اسکیمای ۴ حوزه‌ی رصد (domains) + جدول مجلات | ✅ `db/migration_002_domains_magazines.sql` نوشته شد — کاربر باید در Supabase SQL Editor اجراش کنه، هنوز اجرا نشده |
| نویگیشن فرانت‌اند بر اساس حوزه‌های رصد | ✅ نوار «حوزه‌ی رصد» بالای هر صفحه‌ی داشبورد اضافه شد (`design/ita-monitoring-prototype.html`) — رصد فضای مجازی فعال، بقیه غیرفعال با toast «به‌زودی». نیاز به تست واقعی بعد از اجرای مهاجرت روی Supabase |
| صفحه‌ی «کتابخانه‌ی مجلات» (نمایش عمومی + آپلود مدیر) | ✅ ساخته شد (گرید نمایش + فرم آپلود PDF/چند عکس فقط برای مدیر) — هنوز با داده‌ی واقعی تست نشده، وابسته به اجرای مهاجرت + تست Storage RLS (نکته‌ی عملیاتی #۹) |
| کالکتور رصد اخبار از وب‌سایت‌های خبری | ⏸️ **فعلاً غیرفعال به‌خواست کاربر** — رصد اخبار هم مثل مدارس/تشکل‌ها فقط در نویگیشن به‌عنوان «غیرفعال/به‌زودی» دیده می‌شه؛ هیچ کالکتوری ساخته نمی‌شه تا کاربر دوباره مطرحش کنه |
| رصد فعالیت مدارس / رصد برنامه تشکل‌ها | ⏸️ **فعلاً غیرفعال به‌خواست کاربر** — در نویگیشن به‌عنوان «غیرفعال/به‌زودی» دیده می‌شن، نه پیاده‌سازی کامل. تصمیم نهایی (فایل اکسل، ساختار جدول) به بعد موکول شد |
| UI ویرایش نام دسته‌بندی‌های موضوعی | ❌ هنوز ساخته نشده |

## نکات مهم عملیاتی (حتماً بخون قبل از کار)

1. **دسترسی push گیت‌هاب از طریق ابزارهای MCP گاهی کار نمی‌کنه** (خطای «Resource not accessible by integration»). وقتی این اتفاق افتاد، راه‌حل: کاربر مستقیم از طریق وب‌سایت گیت‌هاب (Upload files / Create new file / Edit) فایل‌ها رو دستی commit می‌کنه. این کند و پرخطاست، پس قبل از هر تغییر بزرگ، اول `git push` رو تست کن؛ اگه کار کرد مستقیم پوش کن، وگرنه فایل رو برای آپلود دستی به کاربر بده.
2. **باگ تکراری: هنگام دانلود فایل از طریق SendUserFile و آپلود دوباره در گیت‌هاب، خط‌تیره‌های اسم فایل حذف می‌شن** (`ita-monitoring-prototype.html` → `itamonitoringprototype.html`). این مشکل سمت سیستم/مرورگر کاربره، نه گیت‌هاب. بعد از هر آپلود دستی، حتماً اسم فایل روی گیت‌هاب رو با `get_file_contents` چک کن.
3. **رمزهای عبور/کلیدهای حساس هرگز نباید توی چت گفته بشن** — `ADMIN_PASSWORD`، `TG_SESSION`، `TG_API_HASH`، رمز دیتابیس Supabase و JWT secret همه به‌عنوان GitHub Secrets مستقیم توسط کاربر تنظیم شدن، من (Claude) هیچ‌کدومشون رو نمی‌دونم و نباید بخوام بدونم.
4. **`SUPABASE_URL` و `SUPABASE_ANON_KEY` محرمانه نیستن** — همین دوتا مستقیم توی کد فرانت‌اند (`design/ita-monitoring-prototype.html`) هاردکد شدن، چون anon key عمومیه و RLS امنیتش رو تضمین می‌کنه.
5. **زمان‌بندی خودکار GitHub Actions فقط از روی نسخه‌ی موجود در شاخه‌ی `main` اجرا می‌شه**، نه شاخه‌ی feature. برای تست دستی هر ورک‌فلو (`workflow_dispatch`)، شاخه رو باید `main` انتخاب کرد.
6. کاربر با گیت/GitHub خیلی راحت نیست — هر مرحله (کجا کلیک کنه، چی بنویسه) باید دقیق و مرحله‌به‌مرحله توضیح داده بشه.
7. کاربر یک اسکیل پروژه‌ای به اسم `ui-ux-pro-max` نصب کرده (`.claude/skills/ui-ux-pro-max/`). پیشنهادهای رنگ/فونتِ خودکارش با هویت بصری تأییدشده‌ی این پروژه (سرمه‌ای #16202a + هلویی #EAB393، فونت فارسی IRANSansX) نمی‌خونه — از چک‌لیست‌های تخصصی‌ش (دسترس‌پذیری، تعامل، تایپوگرافی) استفاده کن، نه پالت رنگی خودکارش.
8. **`db/migration_002_domains_magazines.sql`** رو کاربر هنوز روی Supabase اجرا نکرده — قبل از هر کاری که به جدول `domains` یا `magazines` نیاز داره (مثلاً UI کتابخانه‌ی مجلات)، مطمئن شو این مهاجرت اجرا شده، وگرنه فرانت‌اند با «relation does not exist» خطا می‌ده.
9. **Storage RLS policyهای bucket `magazines`** (در migration_002) بر پایه‌ی همون الگوی نقش‌های سفارشی `app_admin`/`app_viewer` نوشته شدن که برای PostgREST جواب می‌ده، ولی مسیر احراز هویت Supabase Storage با PostgREST یکی نیست — تا وقتی کاربر یک آپلود واقعی رو تست نکرده، مطمئن نباش که این policyها همون‌طوری که فکر می‌کنیم کار می‌کنن. اگه آپلود ۴۰۳ داد، این اولین جای مشکوکه.

## قدم بعدی

فاز اول (بازطراحی حوزه‌های رصد) شروع شده:
- ✅ تصمیم معماری نهایی شد: ۴ حوزه‌ی رصد (فضای مجازی، اخبار، مدارس، تشکل‌ها) + بخش تحلیلی/کتابخانه‌ی مجلات
- ✅ `db/migration_002_domains_magazines.sql` نوشته شد (جدول domains، جدول magazines، پلتفرم website، Storage bucket)
- ✅ **تصمیم جدید کاربر**: از ۴ حوزه، فعلاً فقط **رصد فضای مجازی** فعاله. رصد اخبار، رصد فعالیت مدارس، و رصد برنامه تشکل‌ها هرسه **غیرفعال** موندن — فقط به‌عنوان آیتم نویگیشن با نشان «غیرفعال/به‌زودی» نمایش داده می‌شن، بدون کالکتور/صفحه/جدول واقعی. هیچ‌کدوم رو خودسرانه شروع نکن؛ منتظر باش کاربر دوباره مطرحش کنه (یا فایل اکسل مدارس/تشکل‌ها رو بده، یا لیست سایت‌های خبری رو بده).
- ✅ نویگیشن فرانت‌اند + کتابخانه‌ی مجلات پیاده‌سازی شد (بخش «نویگیشن فرانت‌اند» و «کتابخانه‌ی مجلات» در جدول بالا) — **هنوز با Supabase واقعی تست نشده**، چون کاربر مهاجرت رو اجرا نکرده. اولین قدم جلسه‌ی بعد: از کاربر بپرس مهاجرت رو اجرا کرده یا نه، و اگه کرده، بخش لاگین کن و صفحه‌ی «بخش تحلیلی» رو با آپلود واقعی امتحان کن (این‌جاست که می‌فهمیم Storage RLS درست کار می‌کنه یا نه).
- ❌ باقی‌مونده: UI ویرایش نام دسته‌بندی‌های موضوعی (سیاسی/اجتماعی/...) — هنوز شروع نشده، در scope این دور نبود.

بحث «مهاجرت به VPS شخصی در برابر ارتقا به Supabase Pro» برای وقتی که فضای دیتابیس (الان ۵۰۰ مگابایت رایگان) تنگ بشه، هنوز باز مونده — تصمیم گرفته نشده.

# جریان — راهنمای پروژه برای Claude

این فایل به‌صورت خودکار توسط Claude Code خونده می‌شه. هدفش اینه که هر جلسه‌ی جدید (چت جدید) بدون نیاز به توضیح دوباره، بفهمه پروژه چیه، تا کجا پیش رفته، و چی مونده.

## ⚠️ شاخه‌ی کاری — این رو اول بخون

**شاخه‌ی زنده و واقعی این پروژه `claude/new-project-8ekywm`ه، نه `main`.** GitHub Pages از همین شاخه سرو می‌شه:
`https://meghdad158mf.github.io/meghdad/design/ita-monitoring-prototype.html`

چرا این‌طوریه: یه زمانی دو تا سشن Claude مستقل (این یکی + یه سشن دیگه) هم‌زمان و بی‌خبر از هم روی همین پروژه کار می‌کردن — یکی (سشن دیگه) شاخه‌ی `claude/new-project-8ekywm` رو با یه بازطراحی بصری کامل (فونت، تصویر هرو، ورود تیره) جلو برد، اون‌یکی (این سشن) شاخه‌ی `claude/jarian-project-scmdhd` رو با ساختار حوزه‌های رصد. بعد از کشف این تداخل، تصمیم گرفته شد **`claude/new-project-8ekywm` مبنا بمونه** (چون GitHub Pages از قبل بهش وصل بود و ظاهرش بهتر بود) و کار حوزه‌های رصد + کتابخانه‌ی مجلات از نو، با PR، روی همین شاخه پیاده‌سازی بشه. شاخه‌ی `main` عمداً به یه حالت قدیمی قبل از این‌همه ریست شده و **دیگه استفاده نمی‌شه** — روش کار نکن، ازش PR نساز، بهش پوش نکن.

**همه‌ی کار از این به بعد**: یه شاخه‌ی جدید از `origin/claude/new-project-8ekywm` بساز، تغییرات رو بده، پوش کن، و یه Pull Request به‌سمت `claude/new-project-8ekywm` باز کن (نه `main`!) تا کاربر مرجش کنه. **پوش مستقیم به `claude/new-project-8ekywm` توسط سیستم مسدوده** (احتمالاً چون شاخه‌ی یه سشن دیگه محسوب می‌شه) — همیشه از مسیر شاخه‌ی جدید + PR برو، حتی برای تغییرات کوچیک مثل یه فایل مستندات.

## پروژه چیه؟

**«جریان»** یک سامانه‌ی رصد رسانه‌ایه برای **اداره‌ی رصد و راهبری سیاسی‌اجتماعی حوزه‌ی علمیه‌ی خراسان**.

قبل از این پروژه، یک سیستم مستقل و کاملاً کارکننده برای پایش تلگرام از قبل وجود داشت (Python + FastAPI + SQLite + Telethon، روی سیستم شخصی کاربر). این پروژه اون سیستم رو با ایتا یکی کرد و کل زیرساخت رو از «سیستم شخصی» به «کاملاً ابری و رایگان» منتقل کرد.

### ساختار محتوایی: ۴ حوزه‌ی رصد

سایت بر اساس ۴ «حوزه‌ی رصد» سازمان‌دهی می‌شه (کارت‌های صفحه‌ی نخست):

1. **رصد فضای مجازی** — تنها حوزه‌ی فعال. همه‌ی کانال‌های ایتا + تلگرام با هم (`platform in ('eitaa','telegram')`)، فارغ از این‌که خبرگزاری باشن یا شخص. شامل پست‌ها، آنالیز، کانال‌ها و افراد.
2. **رصد اخبار** — ⏸️ غیرفعال به‌خواست کاربر. فقط کارت با نشان «به‌زودی»، کلیک روش toast می‌ده. قرار بود بخشی از وب‌سایت‌های خبری + بخشی از کانال‌های خبرگزاری فضای مجازی باشه — طراحی نگه داشته شده برای وقتی دوباره مطرح بشه.
3. **رصد فعالیت مدارس** — ⏸️ غیرفعال. منبعش قراره یه فایل اکسل باشه که کاربر می‌ده؛ هنوز نداده.
4. **رصد برنامه تشکل‌ها** — ⏸️ غیرفعال. همون مدل اکسل.

علاوه بر این ۴ تا، **«بخش تحلیلی»** یک کارت پنجمه که کاملاً فعال و کاره: **کتابخانه‌ی مجلات** (توضیح کامل پایین‌تر).

هیچ‌کدوم از ۳ تای غیرفعال (اخبار/مدارس/تشکل‌ها) رو خودسرانه پیش نبر — منتظر باش کاربر دوباره مطرحش کنه.

## معماری فعلی

```
GitHub Actions (هر ۲ ساعت، cron — فقط از شاخه‌ی main اجرا می‌شه، نگاه کن به نکته‌ی عملیاتی #۵)
    │
    ├── scripts/collect_eitaa.py     ── اسکرپ HTML صفحات عمومی eitaa.com (بدون نیاز به API)
    └── scripts/collect_telegram.py  ── Telethon، polling دوره‌ای (نه listener زنده)
    │
    ▼
Supabase (Postgres + PostgREST رایگان)
    │  دو نقش Postgres: app_admin / app_viewer
    │  ورود با پسورد مشترک از طریق تابع public.login() (pgcrypto برای هش، pgjwt برای توکن)
    │  RLS: بیننده فقط خواندن، مدیر خواندن/نوشتن
    │  Storage: سه bucket — magazines (خصوصی)، magazine-covers (عمومی)
    ▼
design/ita-monitoring-prototype.html  ── فرانت‌اند تک‌فایلی، مستقیم با fetch به PostgREST/Storage وصله
```

هیچ بک‌اند سفارشی (FastAPI و مشابه) وجود نداره — فرانت‌اند مستقیم با Supabase حرف می‌زنه.

**نکته‌ی مهم:** کالکتورهای GitHub Actions فقط از شاخه‌ی `main` اجرا می‌شن (رفتار پیش‌فرض GitHub)، ولی `main` الان به یه نسخه‌ی قدیمی ریست شده. یعنی خودِ کالکتورها (`collect_eitaa.py`/`collect_telegram.py`) هنوز کار می‌کنن چون تغییری نکردن، ولی اگه یه‌روز خواستی این اسکریپت‌ها رو عوض کنی، باید یا `main` رو به‌روز کنی یا این محدودیت رو با کاربر در میون بذاری.

## ساختار ریپو

```
db/schema.sql                              اسکیمای پایه (channels, posts, categories, app_config, login/change_password) — دست نزن، رمزها رو ریست می‌کنه اگه دوباره کامل اجرا بشه
db/migration_002_domains_magazines.sql     جدول domains + magazines، پلتفرم website، bucket خصوصی magazines
db/migration_003_storage_grants.sql        grant های schema/table سطح storage به app_admin/app_viewer (بدون این، هر bucket جدید 403 می‌ده)
db/migration_004_magazine_sort_delete.sql  ستون sort_order روی magazines
db/migration_005_magazine_covers.sql       bucket عمومی magazine-covers + policy
db/seed_telegram_channels.sql              ۴ کانال پیش‌فرض تلگرام
scripts/collect_eitaa.py                   کالکتور ایتا
scripts/collect_telegram.py                کالکتور تلگرام
scripts/telegram_session_to_string.py      ابزار یک‌بارمصرف محلی
.github/workflows/collect-eitaa.yml
.github/workflows/collect-telegram.yml
design/ita-monitoring-prototype.html       فرانت‌اند کامل (تک‌فایل HTML/CSS/JS)
design/fonts/                              فونت IRANSansX (وریبل + Regular، وصل با @font-face)
design/images/dashboard-hero.webp          تصویرسازی هرو داشبورد (پس‌زمینه‌ی شفاف)
.claude/skills/ui-ux-pro-max/              اسکیل طراحی نصب‌شده توسط یک سشن دیگه (چک‌لیست‌های UI/UX)
```

همه‌ی migration فایل‌ها **باید دستی** توسط کاربر در Supabase SQL Editor اجرا بشن — چیزی خودکار نیست. اگه یه فیچر جدید نیاز به جدول/bucket جدید داره، فایل migration بعدی رو با شماره‌ی بعدی بساز (نه دست‌زدن به قدیمی‌ها).

## جدول‌های دیتابیس (خلاصه)

- **channels**: id, platform (`eitaa`|`telegram`|`website`), username, title, type (`news_agency`|`person`), category_id, active
- **posts**: id, channel_id, platform, platform_post_id, text, media_type, posted_at, scraped_at, views, forwards, link — یکتا روی (channel_id, platform_post_id)
- **categories**: id, name, color — دسته‌بندی موضوعی، محور جدا از حوزه‌ی رصد
- **domains**: id, key (`cyberspace`|`news`|`schools`|`orgs`), name, sort_order — فقط برای رفرنس/مستندسازی، فرانت‌اند فعلاً مستقیم از روی feature-cardهای هاردکد کار می‌کنه، نه از این جدول کوئری می‌گیره
- **magazines**: id, title, issue_no, publish_date, file_type (`pdf`|`images`), files (jsonb، مسیرهای bucket خصوصی magazines)، cover_url (لینک عمومی bucket magazine-covers یا null)، sort_order، uploaded_at
- **app_config**: رمزهای هش‌شده‌ی مدیر/بیننده + رمز JWT (فقط از طریق توابع SECURITY DEFINER قابل‌خوندنه)

## بخش تحلیلی — کتابخانه‌ی مجلات (کامل پیاده‌سازی شده)

این تنها بخش کاملاً جدید و کاملاً فعاله از کل کار امروز. شامل:

- **صفحه‌ی نخست**: یه ویجت کاروسل افقی (`#mag-carousel-home`) زیر کارت‌های ویژگی — جلد مجلات به‌شکل کارت‌های مستطیلی، با فلش چپ/راست برای اسکرول (جهت فلش‌ها حدسیه، هنوز تأیید نشده — نکته‌ی عملیاتی #۱۰ رو ببین).
- **صفحه‌ی اختصاصی** (`sec-reports`، عنوانش «بخش تحلیلی — کتابخانه‌ی مجلات»): گرید کامل مجلات + پنل بارگذاری (فقط مدیر).
- **بارگذاری**: فایل اصلی (PDF یا چند عکس) می‌ره توی bucket خصوصی `magazines`؛ تصویر جلد (اختیاری) می‌ره توی bucket عمومی `magazine-covers` (تا کاروسل بدون signed URL جداگانه برای هر کارت رندر بشه).
- **نمایش**: به‌جای دانلود مستقیم/`window.open`، یه پنجره‌ی مودال تقریباً تمام‌صفحه (`#magazine-viewer`) باز می‌شه — PDF با iframe (toolbar مخفی)، عکس با `<img>` (راست‌کلیک/درگ غیرفعال). **این جلوگیری صددرصدی از دانلود نیست** — فقط جلوی کاربر عادی رو می‌گیره، نه کاربر فنی مصمم (اسکرین‌شات، DevTools). این محدودیت به کاربر گفته شده.
- **مدیریت (فقط مدیر)**: حذف مجله (فایل‌های Storage + ردیف دیتابیس با هم پاک می‌شن)، جابجایی ترتیب با دکمه‌های بالا/پایین (`sort_order`).

## جزئیات مهم طراحی (برای هماهنگی سشن‌های بعدی)

- **سیستم بصری کل اپ**: «کارت روشن شناور روی بوم تیره‌ی یکپارچه». هم صفحه‌ی ورود هم داشبورد از همین یک گرادیان تیره (`var(--primary)` به `var(--primary-2)`) به‌عنوان پس‌زمینه استفاده می‌کنن؛ هر پنل/کارت محتوا (`.panel`, `.feature-card`, `.stat-card`, `.magazine-card`, کارت‌های ورود و...) یک سطح روشن (`var(--surface)`) شناور روشه. رنگ اصلی برند (سرمه‌ای #16202a + هلویی #EAB393) عوض نشده.
- **باگ مهمی که رفع شد**: قانون CSS `#view-login{ display:flex }` روی خودِ ID باعث می‌شد صفحه‌ی ورود همیشه نمایش داده بشه، مستقل از کلاس `active`. الان باید `#view-login.active` باشه — اگه جای دیگه‌ای مشابهش دیدی، همون باگه.
- تصویرسازی هرو داشبورد (`design/images/dashboard-hero.webp`) عمداً **پس‌زمینه‌ی شفاف** داره — هیچ‌وقت با فایل تخت‌رنگ جایگزینش نکن.
- بخش «داشبورد» همه‌جا به «صفحه نخست» تغییر نام داده. فقط شناسه‌های داخلی کد (`sec-dashboard`, `switchSection('dashboard')`, کلید `dashboard` در `TITLES`) هنوز انگلیسی/قدیمی‌ان — عمداً دست نخورده.
- کارت‌های حوزه‌های غیرفعال (اخبار/مدارس/تشکل‌ها) از همون کلید‌های داخلی قدیمی استفاده می‌کنن (`overview`/`schools`/`cyberspace` در `TITLES` و `switchSection`) — اسم نمایشی‌شون عوض شده ولی id/key کد نه، عمداً، تا دیف کوچیک بمونه.
- Storage bucketهای خصوصی (`magazines`) با **signed URL کوتاه‌عمر (۶۰ ثانیه)** باز می‌شن؛ bucketهای عمومی (`magazine-covers`) مستقیم با URL ثابت (`/storage/v1/object/public/...`).

## نکات مهم عملیاتی (حتماً بخون قبل از کار)

1. **تمام تغییرات باید از مسیر شاخه‌ی جدید + Pull Request به `claude/new-project-8ekywm` برن** (نه push مستقیم — مسدوده؛ نه PR به `main` — بی‌فایده‌ست). این کندتره از یه push ساده ولی تنها راهیه که کار می‌کنه.
2. **باگ تکراری: هنگام دانلود فایل از طریق SendUserFile و آپلود دوباره در گیت‌هاب، خط‌تیره‌های اسم فایل حذف می‌شن.** برای همین همیشه از مسیر PR/git push برو، نه آپلود دستی، مگه این‌که push هم مسدود بشه.
3. **رمزهای عبور/کلیدهای حساس هرگز نباید توی چت گفته بشن** — `ADMIN_PASSWORD`، `TG_SESSION`، `TG_API_HASH`، رمز دیتابیس Supabase و JWT secret همه GitHub Secrets‌ان، من (Claude) هیچ‌کدومشون رو نمی‌دونم و نباید بخوام بدونم.
4. **`SUPABASE_URL` و `SUPABASE_ANON_KEY` محرمانه نیستن** — هاردکد شدن توی فرانت‌اند، چون anon key عمومیه و RLS امنیتش رو تضمین می‌کنه.
5. **کالکتورهای GitHub Actions فقط از `main` اجرا می‌شن** (که الان قدیمیه — بالا توضیح داده شد).
6. کاربر با گیت/GitHub خیلی راحت نیست — هر مرحله (کجا کلیک کنه، چی بنویسه) باید دقیق و مرحله‌به‌مرحله توضیح داده بشه. برای دیباگ خطاهای Supabase/Storage از کاربر می‌خوایم برنامه‌نویس (F12 → Network → کلیک روی درخواست قرمز → تب Response) رو باز کنه و متن خطا رو بفرسته — این روش تا الان چندبار جواب داده. نکته: DevTools کاربر گاهی پاسخ‌ها رو زود از حافظه پاک می‌کنه («evicted»)؛ اگه شد، بگو لاگ رو Clear کنه و فقط یه‌بار امتحان کنه.
7. کاربر یک اسکیل پروژه‌ای به اسم `ui-ux-pro-max` نصب کرده. پیشنهادهای رنگ/فونتِ خودکارش با هویت بصری این پروژه نمی‌خونه — از چک‌لیست‌های تخصصی‌ش استفاده کن، نه پالت رنگی خودکارش.
8. **برای پیش‌نمایش سریع طرح، از ابزار Artifact استفاده می‌شه**، ولی محدودیت داره: فونت‌های relative path لود نمی‌شن و CSP جلوی فراخوانی Supabase رو می‌گیره. برای نسخه‌ی کاملاً واقعی، همیشه لینک GitHub Pages رو بده.
9. اگه کاربر عکس/فایلی رو مستقیم داخل چت paste کنه (نه پیوست `@`)، روی دیسک قابل‌دسترس نیست — بخواه دوباره به‌عنوان پیوست واقعی بفرسته.
10. **جهت فلش‌های کاروسل مجلات در صفحه‌ی نخست تأیید نشده** — اگه کاربر گفت برعکسه، توی `scrollMagCarousel(dir)` و دو تا `onclick` مربوطه توی HTML علامت `dir` رو برعکس کن.
11. **قبل از ساختن هر Storage bucket جدید**، حتماً یادت باشه علاوه بر RLS policy (مثل الگوی migration_002/005)، grant سطح schema/table هم لازمه (migration_003) — این grant یه‌بار برای کل schema `storage` انجام شده و برای bucketهای بعدی هم کافیه، نیازی به تکرارش نیست؛ فقط RLS policy مخصوص bucket جدید رو بنویس.

## قدم بعدی

- کاربر می‌خواد بعدش بره سراغ **«ترکیب صفحه‌ی نخست»** (چیدمان کلی هرو + کارت‌ها + کاروسل مجلات) — هنوز دقیقاً نگفته چی می‌خواد عوض بشه، منتظر توضیح بیشتر یا مرجع تصویری بمون قبل از شروع.
- ۳ حوزه‌ی غیرفعال (اخبار/مدارس/تشکل‌ها) دست‌نخورده می‌مونن تا کاربر دوباره مطرحشون کنه.
- UI ویرایش نام دسته‌بندی‌های موضوعی (سیاسی/اجتماعی/...) هنوز ساخته نشده.
- تحلیل هوش مصنوعی واقعی (n8n + Claude API) هنوز فقط UI نمونه‌ست.
- بحث «مهاجرت به VPS شخصی در برابر ارتقا به Supabase Pro» (وقتی فضای دیتابیس ۵۰۰ مگابایتی تنگ بشه) باز مونده.

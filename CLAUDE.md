# جریان — راهنمای پروژه برای Claude

این فایل به‌صورت خودکار توسط Claude Code خونده می‌شه. هدفش اینه که هر جلسه‌ی جدید (چت جدید) بدون نیاز به توضیح دوباره، بفهمه پروژه چیه، تا کجا پیش رفته، و چی مونده.

## پروژه چیه؟

**«جریان»** یک سامانه‌ی رصد رسانه‌ایه برای **اداره‌ی رصد و راهبری سیاسی‌اجتماعی حوزه‌ی علمیه‌ی خراسان**. دو زیرماژول داره که قراره زیر یک سقف مشترک باشن:
1. **پایش پیام‌رسان ایتا** — کانال‌های عمومی ایتا (فعلاً ۲۲ کانال، عمدتاً خبرگزاری‌ها و شخصیت‌های حوزه‌ی خراسان)
2. **پایش تلگرام** — کانال‌های تلگرام (فعلاً فقط ۴ کانال خبرگزاری عمومی به‌عنوان پیش‌فرض؛ لیست واقعی هنوز کامل نشده)

قبل از این پروژه، یک سیستم مستقل و کاملاً کارکننده برای پایش تلگرام از قبل وجود داشت (Python + FastAPI + SQLite + Telethon، روی سیستم شخصی کاربر). این پروژه اون سیستم رو با ایتا یکی کرد و کل زیرساخت رو از «سیستم شخصی» به «کاملاً ابری و رایگان» منتقل کرد.

## نیازهای اصلی کاربر (زمینه‌ی تصمیم‌های معماری)

- **بدون سیستم شخصی** — همه‌چیز باید ابری/میزبانی‌شده باشه
- **به‌روزرسانی دوره‌ای** — حداقل هر چند ساعت یک‌بار (الان: هر ۲ ساعت)
- **کمترین هزینه** — ترجیحاً رایگان
- **سرعت بالا** — دیتابیس واقعی، نه فایل ساده
- **قابلیت آنالیزهای متنوع** — SQL واقعی، نه محدود به گزارش‌های از پیش تعریف‌شده
- کانال‌ها باید نوع داشته باشن: **خبرگزاری** یا **شخصیت/فرد**
- هر کانال/شخصیت باید بتونه **صفحه‌ی آنالیز اختصاصی خودش** رو داشته باشه (هنوز پیاده نشده — نگاه کن به «موارد باز»)

## معماری فعلی

```
GitHub Actions (هر ۲ ساعت، cron)
    │
    ├── scripts/collect_eitaa.py     ── اسکرپ HTML صفحات عمومی eitaa.com (بدون نیاز به API)
    └── scripts/collect_telegram.py  ── Telethon، polling دوره‌ای (نه listener زنده)
    │
    ▼
Supabase (Postgres + PostgREST رایگان)
    │  دو نقش Postgres: app_admin / app_viewer
    │  ورود با پسورد مشترک از طریق تابع public.login() (pgcrypto برای هش، pgjwt برای توکن)
    │  RLS: بیننده فقط خواندن، مدیر خواندن/نوشتن
    ▼
design/ita-monitoring-prototype.html  ── فرانت‌اند تک‌فایلی، مستقیم با fetch به PostgREST وصله
```

هیچ بک‌اند سفارشی (FastAPI و مشابه) وجود نداره — فرانت‌اند مستقیم با Supabase حرف می‌زنه.

## ساختار ریپو

```
db/schema.sql                          اسکیمای کامل (جدول‌ها، RLS، توابع login/change_password)
db/seed_telegram_channels.sql          ۴ کانال پیش‌فرض تلگرام
scripts/collect_eitaa.py               کالکتور ایتا (اجرا هر ۲ ساعت با GitHub Actions)
scripts/collect_telegram.py            کالکتور تلگرام (اجرا هر ۲ ساعت)
scripts/telegram_session_to_string.py  ابزار یک‌بارمصرف محلی برای گرفتن StringSession از session قدیمی
.github/workflows/collect-eitaa.yml
.github/workflows/collect-telegram.yml
design/ita-monitoring-prototype.html   فرانت‌اند کامل (نقشه‌ی سایت، ورود، داشبورد، پست‌ها، آنالیز، کانال‌ها، خروجی، تنظیمات)
.claude/skills/ui-ux-pro-max/          اسکیل طراحی نصب‌شده توسط یک سشن دیگه (چک‌لیست‌های UI/UX)
```

## جدول‌های دیتابیس (خلاصه)

- **channels**: id, platform (`eitaa`|`telegram`), username, title, type (`news_agency`|`person`), category_id, active
- **posts**: id, channel_id, platform, platform_post_id, text, media_type, posted_at, scraped_at, views, forwards, link — یکتا روی (channel_id, platform_post_id)
- **categories**: id, name, color
- **app_config**: رمزهای هش‌شده‌ی مدیر/بیننده + رمز JWT (فقط از طریق توابع SECURITY DEFINER قابل‌خوندنه، هیچ‌وقت مستقیم API نمی‌خوره)

## وضعیت فعلی (مرحله‌به‌مرحله)

| بخش | وضعیت |
|---|---|
| طراحی UI/UX | ✅ تکمیل، یک بار هم صیقل‌کاری با ui-ux-pro-max |
| اسکیمای دیتابیس + احراز هویت دوسطحی | ✅ روی Supabase مستقر و تست‌شده |
| کالکتور ایتا | ✅ فعال، هر ۲ ساعت اجرا می‌شه، داده‌ی واقعی می‌گیره |
| کالکتور تلگرام | ✅ فعال، فقط ۴ کانال پیش‌فرض داره |
| اتصال فرانت‌اند به داده‌ی واقعی | ✅ کامل (نه دیگه mock data) |
| صفحه‌ی آنالیز اختصاصی هر کانال | ❌ هنوز نه — فقط آنالیز کلی جمعی هست |
| لیست واقعی کانال‌های تلگرام | ❌ فقط ۴ خبرگزاری عمومی پیش‌فرضه؛ کانال‌های واقعی حوزه هنوز اضافه نشدن |
| تحلیل هوش مصنوعی | ❌ فقط UI نمونه — پایپ‌لاین واقعی (n8n + Claude API) ساخته نشده |
| خروجی Excel | ❌ فقط CSV/JSON واقعی کار می‌کنه؛ دکمه‌ی Excel toast «به‌زودی» می‌ده |
| مهاجرت داده‌ی تاریخی تلگرام (messages.db قدیمی، ۱۸۳ مگابایت SQLite) | ❌ کاری روش انجام نشده |

## نکات مهم عملیاتی (حتماً بخون قبل از کار)

1. **دسترسی push گیت‌هاب از طریق ابزارهای MCP گاهی کار نمی‌کنه** (خطای «Resource not accessible by integration»). وقتی این اتفاق افتاد، راه‌حل: کاربر مستقیم از طریق وب‌سایت گیت‌هاب (Upload files / Create new file / Edit) فایل‌ها رو دستی commit می‌کنه. این کند و پرخطاست، پس قبل از هر تغییر بزرگ، اول `git push` رو تست کن؛ اگه کار کرد مستقیم پوش کن، وگرنه فایل رو برای آپلود دستی به کاربر بده.
2. **باگ تکراری: هنگام دانلود فایل از طریق SendUserFile و آپلود دوباره در گیت‌هاب، خط‌تیره‌های اسم فایل حذف می‌شن** (`ita-monitoring-prototype.html` → `itamonitoringprototype.html`). این مشکل سمت سیستم/مرورگر کاربره، نه گیت‌هاب. بعد از هر آپلود دستی، حتماً اسم فایل روی گیت‌هاب رو با `get_file_contents` چک کن.
3. **رمزهای عبور/کلیدهای حساس هرگز نباید توی چت گفته بشن** — `ADMIN_PASSWORD`، `TG_SESSION`، `TG_API_HASH`، رمز دیتابیس Supabase و JWT secret همه به‌عنوان GitHub Secrets مستقیم توسط کاربر تنظیم شدن، من (Claude) هیچ‌کدومشون رو نمی‌دونم و نباید بخوام بدونم.
4. **`SUPABASE_URL` و `SUPABASE_ANON_KEY` محرمانه نیستن** — همین دوتا مستقیم توی کد فرانت‌اند (`design/ita-monitoring-prototype.html`) هاردکد شدن، چون anon key عمومیه و RLS امنیتش رو تضمین می‌کنه.
5. **زمان‌بندی خودکار GitHub Actions فقط از روی نسخه‌ی موجود در شاخه‌ی `main` اجرا می‌شه**، نه شاخه‌ی feature. برای تست دستی هر ورک‌فلو (`workflow_dispatch`)، شاخه رو باید `main` انتخاب کرد.
6. کاربر با گیت/GitHub خیلی راحت نیست — هر مرحله (کجا کلیک کنه، چی بنویسه) باید دقیق و مرحله‌به‌مرحله توضیح داده بشه.
7. کاربر یک اسکیل پروژه‌ای به اسم `ui-ux-pro-max` نصب کرده (`.claude/skills/ui-ux-pro-max/`). پیشنهادهای رنگ/فونتِ خودکارش با هویت بصری تأییدشده‌ی این پروژه (سرمه‌ای #16202a + هلویی #EAB393، فونت فارسی IRANSansX) نمی‌خونه — از چک‌لیست‌های تخصصی‌ش (دسترس‌پذیری، تعامل، تایپوگرافی) استفاده کن، نه پالت رنگی خودکارش.

## قدم بعدی

سه گزینه مطرحه (کاربر هنوز تصمیم نگرفته):
1. صفحه‌ی آنالیز اختصاصی هر کانال/شخصیت (پیشنهاد Claude)
2. تکمیل لیست واقعی کانال‌های تلگرام
3. تحلیل هوش مصنوعی واقعی (n8n + Claude API)

همچنین بحث «مهاجرت به VPS شخصی در برابر ارتقا به Supabase Pro» برای وقتی که فضای دیتابیس (الان ۵۰۰ مگابایت رایگان) تنگ بشه، باز مونده — تصمیم گرفته نشده.
